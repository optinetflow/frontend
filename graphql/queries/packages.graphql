query GetPackages($input: GetPackageInput!) {
    packages(data: $input) {
        id
        price
        discountedPrice
        traffic
        expirationDays
        userCount
        category
    }
}